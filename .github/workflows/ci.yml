name: CI

on:
  merge_group:
  pull_request:
  push:
    branches:
      - main

permissions:
  contents: read
  pull-requests: read

jobs:
  changes:
    name: Detect changes
    runs-on: ubuntu-latest
    outputs:
      app: ${{ steps.routes.outputs.app }}
      docs: ${{ steps.routes.outputs.docs }}
      landing: ${{ steps.routes.outputs.landing }}
    steps:
      - uses: actions/checkout@v5
      - id: files
        name: Collect changed files
        uses: actions/github-script@v9
        with:
          result-encoding: string
          script: |
            if (context.eventName !== 'pull_request') {
              return JSON.stringify(['__all__']);
            }

            const files = await github.paginate(github.rest.pulls.listFiles, {
              owner: context.repo.owner,
              repo: context.repo.repo,
              pull_number: context.issue.number,
              per_page: 100,
            });
            return JSON.stringify(files.map(({ filename }) => filename));
      - name: Test path router
        run: node --test scripts/ci-paths.test.mjs
      - id: routes
        name: Route CI suites
        env:
          CHANGED_FILES_JSON: ${{ steps.files.outputs.result }}
        run: node scripts/ci-paths.mjs >> "$GITHUB_OUTPUT"

  landing:
    name: Landing CI
    needs: changes
    if: needs.changes.outputs.landing == 'true'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-node@v5
        with:
          node-version: '22.22.3'
          cache: npm
      - run: npm ci
      - run: npm run check
      - run: npx eslint src/web/landing --max-warnings=0
      - run: npm test -- src/web/landing/LandingPage.test.tsx
      - run: npm run knip
      - run: npm run pages:check
      - run: npm run public:readiness

  docs:
    name: Documentation CI
    needs: changes
    if: needs.changes.outputs.docs == 'true'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-node@v5
        with:
          node-version: '22.22.3'
          cache: npm
      - run: npm ci
      - run: npm run check
      - run: node --test scripts/docs-check.test.mjs scripts/prepare-pages.test.mjs scripts/ci-paths.test.mjs
      - run: npm run pages:check
      - run: npm run public:readiness

  app:
    name: Application CI
    needs: changes
    if: needs.changes.outputs.app == 'true'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-node@v5
        with:
          node-version: '22.22.3'
          cache: npm
      - run: npm ci
      - run: npx playwright install --with-deps chromium
      - run: npm run ci

  ci:
    name: ci
    needs:
      - changes
      - docs
      - landing
      - app
    if: always()
    runs-on: ubuntu-latest
    steps:
      - name: Require every selected suite to pass
        env:
          APP_RESULT: ${{ needs.app.result }}
          CHANGES_RESULT: ${{ needs.changes.result }}
          DOCS_RESULT: ${{ needs.docs.result }}
          LANDING_RESULT: ${{ needs.landing.result }}
        run: |
          test "$CHANGES_RESULT" = success
          case "$DOCS_RESULT" in success|skipped) ;; *) exit 1 ;; esac
          case "$LANDING_RESULT" in success|skipped) ;; *) exit 1 ;; esac
          case "$APP_RESULT" in success|skipped) ;; *) exit 1 ;; esac
