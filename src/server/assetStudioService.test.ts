import { describe, expect, it } from 'vitest';

describe.skip('Asset Studio managed service ownership', () => {
  it('requires the omitted managed-service helper', () => {
    expect(true).toBe(true);
  });
});
